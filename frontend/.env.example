# When the UI is deployed separately from the API, set your backend origin (no trailing slash).
# Example: https://meddozer-api.vercel.app
# Leave unset for local dev (Vite proxies /api → http://localhost:3000).
VITE_API_BASE_URL=https://meddozer-ai-assistant-1jje.vercel.app
